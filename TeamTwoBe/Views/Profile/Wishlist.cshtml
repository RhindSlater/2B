@model TeamTwoBe.Models.User
@{
    ViewBag.Title = "Wishlist";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <script type="text/javascript" src="http://code.jquery.com/jquery-1.7.1.min.js"></script>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/lozad/dist/lozad.min.js"></script>
</head>
<body>

    <h2>@Html.ActionLink(Model.Username + "'s Wishlist", "Profile", "Users", new { id = Model.ID }, null)</h2>
    @if (Model.ID.ToString() == Session["UserID"].ToString())
    {
        <h4>@Html.ActionLink("Add card to wishlist", "Wishlist", "Card")</h4>
    }

    <div>
        @if (Model.Wishlist.Count < 1)
        {
            <h4>@Model.Username does not have any cards in their wishlist.</h4>
        }
        else
        {
            <table class="table table-hover table-bordered">
                <tr>
                    <th>Image</th>
                    <th>Cards</th>
                </tr>
                @foreach (var item in Model.Wishlist)
                {
                    <tr>
                        <td style="text-align: center"><div data-tilt data-tilt-glare="true" style="display:inline-block;"><img height="306" width="210" class="lozad" src="https://upload.wikimedia.org/wikipedia/commons/c/c0/Blank.gif" data-src="@item.image_url" /></div></td>
                        <td>
                            <h4><b>Name:</b> @item.name</h4>
                            <p><b>Card Type:</b> @item.Cardtype.Name</p>
                            <p><b>Set:</b> @item.print_tag</p>
                            <p><b>Rarity:</b> @item.rarity</p>
                            <p><b>Average Price:</b> @item.average.ToString("c2")</p>
                            @if (Model.ID.ToString() == Session["UserID"].ToString())
                    {
                                <a>@Html.ActionLink("Remove from wishlist", "removeWishlist", new { id = item.ID }, new { @class = "btn btn-primary" })</a>
                            }
                            else
                            {
                                <a>@Html.ActionLink("Add to Wishlist", "addCardToWishlist", "Card", new { id = item.ID }, new { @class = "btn btn-primary" })</a>
                            }
                        </td>
                    </tr>
                }
            </table>
        }
    </div>

    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/vanilla-tilt/1.7.0/vanilla-tilt.babel.min.js"></script>
    <script type="text/javascript" src="~/Scripts/lazyloading.js"></script>
    <script type="text/javascript" src="~/Scripts/lazyloading2.js"></script>
    <script src="https://code.jquery.com/jquery-3.1.0.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/lozad/dist/lozad.min.js"></script>
</body>

